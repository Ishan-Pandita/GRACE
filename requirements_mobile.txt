# Mobile-optimized requirements for prompt compression system
torch>=2.0.0
transformers>=4.40.0
sentence-transformers>=2.7.0
networkx>=3.2.1
numpy>=1.24.0
psutil>=5.9.0
nltk>=3.8.0

# Optional for model quantization
# bitsandbytes>=0.41.0
# accelerate>=0.20.0

# Optional for TensorFlow Lite conversion
# tensorflow>=2.14.0

# Note: The following are HuggingFace model identifiers, not pip packages.
# They are downloaded automatically at runtime:
#   - t5-small (abstractive summarization)
#   - distilroberta-base (lightweight causal model)

# Development dependencies
pytest>=7.4.0
black>=23.0.0
flake8>=6.0.0
